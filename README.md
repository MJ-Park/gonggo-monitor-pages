# 공공지원사업 공고 인덱스

Grok Bot `gonggo-monitor` dry-run/live outbox snapshot for browser viewing.

- `index.html` — searchable index
- `index.csv` — same rows as CSV
- `summary.md` — daily summary

Updated from the monitor pipeline; not a live scrape in the browser.
